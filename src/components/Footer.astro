---
import CTABanner from './CTABanner.astro';
import siteSettings from '../data/site-settings.json';

export interface Props {
  lang?: 'fr' | 'en';
}

const { lang = 'fr' } = Astro.props as Props;
const isEnglish = lang === 'en';
const pathname = Astro.url.pathname;
const shouldShowCta = pathname !== '/contact/' && pathname !== '/en/contact/';
---

{shouldShowCta && <CTABanner lang={isEnglish ? 'en' : 'fr'} />}

<footer class="footer">
  <div class="container footer-content">
    <div class="footer-col">
      <img class="footer-brand" src={siteSettings.brandImage} alt={siteSettings.brandName} loading="lazy" decoding="async" />
      <p>{isEnglish ? siteSettings.footerDescription.en : siteSettings.footerDescription.fr}</p>
    </div>
    <div class="footer-col">
      <h4>{isEnglish ? 'Contact' : 'Contact'}</h4>
      <p>
        <strong>{isEnglish ? 'Phone:' : 'Téléphone:'}</strong> <a href={`tel:${siteSettings.phone.href}`}>{siteSettings.phone.display}</a><br>
        <a href={siteSettings.squareBookingUrl} target="_blank" rel="noopener" class="footer-square-link">{isEnglish ? 'Book online' : 'Réserver en ligne'}</a><br>
        <strong>Email:</strong> <a href={`mailto:${siteSettings.email}`}>{siteSettings.email}</a><br>
        <strong>{isEnglish ? 'Insurance:' : 'Assurance:'}</strong> {isEnglish ? '$2M garage and liability' : 'Garage et civile 2 millions'}
      </p>
    </div>
    <div class="footer-col">
      <h4>{isEnglish ? 'Services' : 'Services'}</h4>
      <ul>
        <li><a href={isEnglish ? '/en/services/inspection/' : '/services/inspection/'}>{isEnglish ? 'Inspection' : 'Inspection'}</a></li>
        <li><a href={isEnglish ? '/en/services/maintenance/' : '/services/entretien/'}>{isEnglish ? 'Maintenance' : 'Entretien'}</a></li>
        <li><a href={isEnglish ? '/en/services/repair/' : '/services/reparation/'}>{isEnglish ? 'Repair' : 'Réparation'}</a></li>
        <li><a href={isEnglish ? '/en/services/detailing/' : '/services/esthetique/'}>{isEnglish ? 'RV Detailing' : 'Esthétique VR'}</a></li>
        <li><a href={isEnglish ? '/en/services/auto-truck-detailing/' : '/services/esthetique-auto-camion/'}>{isEnglish ? 'Auto Detailing' : 'Esthétique auto'}</a></li>
        <li><a href={isEnglish ? '/en/sales/' : '/vente/'}>{isEnglish ? 'Sales' : 'Vente'}</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>{isEnglish ? 'Resources' : 'Ressources'}</h4>
      <ul>
        <li><a href={isEnglish ? '/en/resources/rv-water-intrusion/' : '/ressources/infiltration-eau-vr/'}>{isEnglish ? 'Water intrusion' : "Infiltration d'eau"}</a></li>
        <li><a href={isEnglish ? '/en/resources/twice-yearly-rv-maintenance/' : '/ressources/entretien-vr-deux-fois-par-an/'}>{isEnglish ? 'Recommended maintenance' : "Entretien recommandé"}</a></li>
        <li><a href={isEnglish ? '/en/privacy/' : '/privacy/'}>{isEnglish ? 'Privacy Policy' : 'Politique de confidentialité'}</a></li>
        <li><a href={isEnglish ? '/en/service-areas/' : '/zones-desservies/'}>{isEnglish ? 'Service Areas' : 'Zones desservies'}</a></li>
        <li><a href={isEnglish ? '/en/faq/' : '/faq/'}>FAQ</a></li>
        <li><a href={isEnglish ? '/en/site-map/' : '/plan-du-site/'}>{isEnglish ? 'Site Map' : 'Plan du site'}</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>{isEnglish ? 'Follow Us' : 'Nous Suivre'}</h4>
      <a href={siteSettings.facebookUrl} target="_blank" rel="noopener">
        <i class="fab fa-facebook"></i> Facebook
      </a>
      <br />
      <a href={siteSettings.tiktokUrl} target="_blank" rel="noopener">
        <i class="fab fa-tiktok"></i> TikTok
      </a>
    </div>
  </div>
  <div class="footer-bottom">
    <div class="container">
      <p>&copy; 2026 {siteSettings.brandName}. {isEnglish ? 'All rights reserved.' : 'Tous droits réservés.'}</p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--brand-navy);
    color: white;
    padding: 3rem 0 0;
    margin-top: 0;
  }

  .footer-content {
    display: grid;
    grid-template-columns: minmax(220px, 1.1fr) minmax(220px, 1fr) minmax(180px, 0.9fr) minmax(220px, 1fr) minmax(180px, 0.9fr);
    gap: 2rem;
    padding: 2rem;
    align-items: start;
  }

  .footer-brand {
    height: 68px;
    width: auto;
    display: block;
    margin-bottom: 1rem;
    background: white;
    border-radius: 10px;
    padding: 0.35rem;
  }

  .footer-col h4 {
    color: var(--brand-gold);
    text-transform: uppercase;
    margin-bottom: 1rem;
  }

  .footer-col a {
    color: white;
  }

  .footer-square-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 40px;
    margin: 0.35rem 0;
    padding: 0.5rem 1rem;
    border-radius: 999px;
    background: var(--brand-gold);
    color: var(--brand-navy-dark) !important;
    border: 2px solid var(--brand-gold);
    font-weight: 700;
    text-decoration: none;
  }

  .footer-square-link:hover {
    text-decoration: none;
    background: #e1c317;
    border-color: #e1c317;
  }

  .footer-col ul {
    list-style: none;
  }

  .footer-col ul li {
    margin: 0.5rem 0;
  }

  .footer-bottom {
    background: var(--brand-navy-dark);
    padding: 1rem;
    text-align: center;
    border-top: 3px solid var(--brand-green);
  }

  .footer-bottom a {
    color: var(--brand-gold);
  }

  @media (max-width: 768px) {
    .footer-content {
      grid-template-columns: 1fr;
      gap: 1.1rem;
      padding: 1.2rem 1rem;
    }

    .footer {
      padding-top: 1.4rem;
    }

    .footer-brand {
      height: 52px;
      margin-bottom: 0.55rem;
    }

    .footer-col h4 {
      margin-bottom: 0.5rem;
      font-size: 1rem;
    }

    .footer-col ul li {
      margin: 0.25rem 0;
    }

    .footer-col p,
    .footer-col li,
    .footer-col a {
      font-size: 0.94rem;
    }

    .footer-square-link {
      width: 100%;
      min-height: 38px;
      padding: 0.55rem 0.9rem;
    }

    .footer-bottom {
      padding: 0.7rem 1rem;
    }
  }
</style>
